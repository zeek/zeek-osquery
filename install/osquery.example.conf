{
  // Bro-Osquery option to configure the extension
  "bro": {
    // The IP and port of the Bro endpoint.
    "bro_ip": "",
    //"bro_port": "9999",

    // The predefined unique ID of osquery host.
    // If this is not set at startup, the osquery uuid is applied.
    // It is derived from mainboard and persistent over reboots.
    //"uid": "",

    // The predefined groups of osquery host.
    // Groups can be assigned at runtime via bro.
    "groups": {
    //    "group1": "geo/de/hamburg",
    //    "group2": "orga/uhh/cs/iss"
        }
  },

  // Configure the daemon below:
  "options": {
    // Select the osquery config plugin.
    "config_plugin": "filesystem",

    // Select the osquery logging plugin.
    "logger_plugin": "bro",

    // Enable debug or verbose debug output when logging.
    "verbose": "true"
  },

  // Define a schedule of queries:
  "schedule": {
  },

  // Decorators are normal queries that append data to every query.
  "decorators": {
  },

  "packs": {
  }
}
